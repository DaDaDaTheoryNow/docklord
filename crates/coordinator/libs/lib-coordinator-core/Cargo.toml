[package]
name = "lib-coordinator-core"
version = "0.1.0"
edition = "2024"

[dependencies]
proto = { path = "../../../proto" }

prost = "0.12"
tonic = "0.11"
tokio = { version = "1", features = ["full"] }
dashmap = "6.1.0"

[lints]
workspace = true

[build-dependencies]
tonic-build = "0.11"
